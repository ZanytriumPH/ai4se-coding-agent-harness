# .dockerignore — keep the image lean AND never bake a credential (§3.1).
.git
.gitignore
**/__pycache__
**/*.pyc
dist/
build/
*.egg-info/
.env
.env.*
.claude/
*.pen
# keep: src/, demo/, tests/, webui/static/, config.yaml, Makefile, pyproject.toml
